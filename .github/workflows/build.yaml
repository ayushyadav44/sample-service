name: Build and Push to ECR

on:
  push:
    branches:
      - master
  wokdlow_dispatch:
    inputs:
      env:
        description: "Deployment environment"
        required: true
        default: "staging"
        type: string
      buildEnv:
        description: "Build environment"
        required: true
        default: "staging"
        type: string
      service:
        description: "Service name"
        required: true
        default: "sample-service"
        type: string
      dfName:
        description: "Dockerfile to use for build"
        required: true
        default: "Dockerfile"
        type: string

env:
  AWS_REGION: ap-south-1

env:
  AWS_REGION: ap-south-1
  AWS_DEFAULT_REGION: ap-south-1

jobs:
  build:
    runs-on: arc-runner-set
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set image tag
        id: tag
        run: |
          COMMIT_ID=$(git rev-parse --short HEAD)
          TAG="${COMMIT_ID}-${{ github.run_number }}"

          # Use input if manually triggered, fall back to secret
          ENV="${{ github.event.inputs.environment || variables.ENV }}"
          SERVICE="${{ github.event.inputs.service || variables.SERVICE }}"

          IMAGE="${{ variables.ECR_REPO }}/${ENV}/${SERVICE}:${TAG}"
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "image=${IMAGE}" >> $GITHUB_OUTPUT
          echo "env=${ENV}" >> $GITHUB_OUTPUT

      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ github.event.inputs.dockerfile_name || variables.DOCKERFILE_NAME || 'Dockerfile' }}
          push: true
          tags: ${{ steps.tag.outputs.image }}
          build-args: |
            BUILD_ENV=${{ github.event.inputs.build_env || variables.BUILD_ENV }}
            MAX_INCOMPLETE_EVENT_SIZE=51200

      - name: Summary
        run: |
          echo "### Build complete" >> $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ steps.tag.outputs.image }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** \`${{ steps.tag.outputs.tag }}\`" >> $GITHUB_STEP_SUMMARY
